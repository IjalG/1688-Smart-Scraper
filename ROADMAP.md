# 发展路线

本文档记录 `1688-Smart-Scraper` 的产品定位、优先级原则与分阶段演进计划。

## 1. 产品定位

这个项目最有价值的点不是“更强的爬虫”，而是：

**人工浏览 + 人工筛选 + 一键整理成可采购清单**

更准确的产品表述：

- 现在：一键提取当前页商品数据
- 目标：把 1688 浏览结果，快速整理成可决策的采购清单

推荐叙事闭环：

1. 浏览时收集
2. 收藏夹里筛选
3. 对比后决策
4. 导出给采购 / 上架 / 报价

因此，项目应持续发展成：

**给 1688 采购用的本地选品工作台**

而不是全自动批量采集器。

## 2. 设计原则

1. **本地优先**：默认不依赖后端服务，核心链路可离线完成
2. **人工决策优先**：插件负责提效，不替代用户判断；关键字段允许用户在选择页直接校正
3. **规则主路径，智能兜底**：日常提取走本地规则，AI 仅在用户手动触发时介入
4. **轻量可维护**：继续保持纯前端 / 低依赖，不为架构而架构
5. **失败可感知**：抓不到、导出失败、图片失败都要给明确反馈
6. **先做深 1688**：在单站点体验扎实前，不急着扩平台

## 3. 明确不做或后置的方向

以下方向短期不作为主线：

- 全自动翻页 / 批量爬取
- 重型框架重构
- 一上来做 SaaS 后台与账号体系
- 多平台一把梭（淘宝 / 拼多多 / 国际站等）
- 每次抓取都自动调用大模型
- 让 AI 直接批量输出商品表，而不是修复提取规则

原因：

- 风控与维护成本高
- 偏离“人工选品助手”定位
- 会破坏当前轻量、本地、低成本的优势

## 4. 总路线图

### 阶段 1：做稳现有能力

> 状态：v1.7.0 已落地（提取稳定性、选择页改价换图、导出进度与图片尺寸、去重与文档）。

目标：先变成“能天天用、少翻车”的工具。

优先事项：

1. **页面适配稳定性**
   - 选择器多策略回退
   - 提取结果自检（0 条 / 字段大面积缺失）
   - 抓取失败时给出可理解提示
2. **选择页人工校正**
   - 同一商品常有多规格 / 多款式，默认抓到的主图和售价不一定是用户要的
   - 价格：在商品选项卡片上直接可编辑（输入框），修改后同步到导出数据
   - 图片：支持把目标图片拖入卡片缩略图区域替换；同时支持右键菜单选择“替换图片”
   - 校正结果应写回当前列表 / 收藏夹，避免导出后还是旧值
   - 可选扩展：标题等少量字段也可点选编辑，但阶段 1 先保证售价与图片
3. **导出体验**
   - 导出进度（尤其图片下载）
   - 图片尺寸可配置
   - 失败图片可重试或标记“无图”
4. **数据质量**
   - 价格 / 销量清洗
   - 按 `offerId` 去重
   - 收藏夹重复提示
5. **发布与文档**
   - 版本发布与 changelog
   - README 同步当前架构与常见问题

### 阶段 2：选品工作台

目标：从“导出工具”升级为“采购决策工具”。

高价值能力：

1. **字段扩展**
   - 起批量 / 最小起订量
   - 发货地
   - 店铺相关信息
   - 更多可选图片字段
2. **评分与排序**
   - 价格、销量、起批等自定义权重
   - 收藏夹内状态：待看 / 候选 / 淘汰
3. **对比视图**
   - 勾选 2 到 5 个商品横向对比
4. **收藏体系增强**
   - 标签、备注
   - 本地 JSON 备份 / 恢复
5. **导出模板**
   - 采购模板 / 上架模板 / 简报模板
   - 自定义列顺序

### 阶段 3：生态扩展

目标：扩大分发与用户面，但保持产品主线不飘。

可选方向：

1. Chrome Web Store 上架
2. 在 1688 做深后再评估多站点
3. 谨慎考虑轻量云同步（仅收藏 / 备注，不做服务端抓取）
4. AI 辅助增强（标题清洗、类目归类、卖点提炼）作为后置能力

## 5. 版本规划

### v1.7：稳定性、人工校正与导出体验

- 提取失败自检与提示
- 选择页支持直接改售价
- 选择页支持拖入 / 右键替换商品图片
- 校正后的售价与图片进入导出结果
- 导出进度反馈
- 图片尺寸设置
- `offerId` 去重
- 规则提取结果评分

### v1.8：AI 手动修复 + 数据增强

- 抓取失败时展示：
  - `无法正常提取信息？试试 AI 分析模式吧`
- 手动触发 AI 修复提取规则
- 起批量 / 发货地等字段补全
- 收藏备注与状态
- 本地备份 / 恢复

### v1.9：决策能力

- 商品对比
- 自定义排序 / 打分
- 导出模板

### v2.0：工作台成型

- 更完整的选品工作流
- 商店分发与安装体验完善
- 可选增强能力按需接入

## 6. AI 能力定位

### 6.1 原则

AI 是**规则修复器**，不是**日常生产线**。

正确形态：

1. 本地规则抓取（主路径）
2. 用户发现异常
3. 手动点击 AI 分析 / 修复
4. AI 基于精简 DOM 样本生成提取规则
5. 本地 dry-run 验证
6. 验证通过后缓存规则，继续本地提取

错误形态：

- 每次打开页面都自动调模型
- 每次导出都让模型直接吐商品 JSON
- 把整页超大 DOM 无差别发送给模型

### 6.2 为什么采用手动触发

手动触发更适合当前产品：

1. 用户意图明确，不误触发
2. 成本可控
3. 隐私更干净
4. 实现更简单
5. 不破坏“轻量插件”心智

推荐交互：

- 失败提示：`未能识别到商品卡片，页面结构可能已更新`
- 引导文案：`无法正常提取信息？试试 AI 分析模式吧`
- 按钮：`开始 AI 修复`
- 成功：`修复完成，已重新提取并保存规则`
- 失败：`未能自动修复，请稍后重试或反馈页面`

### 6.3 建议架构

```text
content.js
  ├─ Adapter Registry（内置规则 + AI 修复规则）
  ├─ extractByRules()
  ├─ validate(products)
  └─ sampleDOMForRepair()

background.js
  ├─ 调用 LLM（用户配置 API Key）
  ├─ 校验返回的 adapter
  └─ 写入 chrome.storage

popup / select UI
  ├─ 失败引导
  ├─ AI 修复入口
  └─ 清除 AI 规则 / 回滚内置规则
```

### 6.4 Adapter 方向

AI 输出应是可复用规则，而不是一次性商品列表。示意：

```json
{
  "version": "2026-07-11",
  "site": "1688-search",
  "card": ".search-offer-wrapper, [data-renderkey]",
  "fields": {
    "title": [".title-text", ".offer-title", "a[title]"],
    "price": [".price-item", "[class*=price]"],
    "sales": [".col-desc_after", "[class*=sale]"],
    "shop": [".desc-text", "[class*=shop]"],
    "image": ["img.main-img", "img[data-src*=alicdn]", "img[src*=alicdn]"],
    "link": ["a[href*='detail.1688.com/offer']", "a[href*='offerId=']"]
  },
  "filters": {
    "skipCard": [".cardui-adOffer", "[class*=adOffer]"]
  }
}
```

配套要求：

- 只发送精简后的 2 到 3 个样本卡片 DOM
- 修复前 dry-run
- 比旧规则更好才保存
- 保留内置规则，支持一键回滚
- 默认不强制依赖 API Key

### 6.5 AI 之前的两层兜底

在接入 LLM 前，应先具备：

1. **多规则并存**
   - 多套选择器同时尝试
   - 用结果完整度评分择优
2. **启发式提取**
   - 通过 `offer` 链接簇定位商品
   - 同卡片内找 `alicdn` 图片
   - 用文本模式识别价格 / 销量

LLM 放在这两层之后，性价比最高。

## 7. 优先级排序

按投入产出排序：

1. 稳定性：选择器容错、失败可感知、去重
2. 选择页人工校正：可改售价、可替换图片
3. 导出体验：进度、图片尺寸、失败重试
4. 采购字段补全：起批、发货地、店铺信息
5. 手动 AI 修复：页面改版自愈
6. 收藏夹工作流：状态、标签、备注
7. 对比 / 排序
8. 商店上架
9. AI 内容增强 / 多平台 / 云同步

## 8. 近期执行建议

如果按最小推进，建议顺序如下：

1. 规则引擎化：把选择器抽成可热更新 adapter
2. 提取结果评分与失败提示
3. 选择页售价可编辑、图片可拖入 / 右键替换
4. 导出进度与图片尺寸配置
5. 手动 AI 修复入口与 adapter 缓存
6. 收藏备注 / 状态 / 备份

一句话收敛：

**先把 1688 本地选品工作台做稳做深，再用手动 AI 修复应对页面改版，而不是把项目做成自动爬虫或重 API 依赖产品。**

## 9. 当前进度

- **已完成**：阶段 1 / v1.7.0
- **下一步**：阶段 2 之前，优先推进 v1.8 的规则引擎化与“手动 AI 分析模式”入口文案与架构预埋
