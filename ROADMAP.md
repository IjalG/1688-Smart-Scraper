# 发展路线

本文档记录 `1688-Smart-Scraper` 的产品定位、优先级原则与分阶段演进计划。

## 1. 产品定位

这个项目最有价值的点不是“更强的爬虫”，而是：

**人工浏览 + 人工筛选 + 一键整理成可采购清单**

更准确的产品表述：

- 现在：一键提取当前页商品数据
- 目标：把 1688 浏览结果，快速整理成可决策的采购清单

推荐叙事闭环：

1. 浏览时收集
2. 收藏夹里筛选
3. 对比后决策
4. 导出给采购 / 上架 / 报价

因此，项目应持续发展成：

**给 1688 采购用的本地选品工作台**

而不是全自动批量采集器。

## 2. 设计原则

1. **本地优先**：默认不依赖后端服务，核心链路可离线完成
2. **人工决策优先**：插件负责提效，不替代用户判断；关键字段允许用户在选择页直接校正
3. **规则主路径，智能兜底**：日常提取走本地规则，AI 仅在用户手动触发时介入
4. **轻量可维护**：继续保持纯前端 / 低依赖，不为架构而架构
5. **失败可感知**：抓不到、导出失败、图片失败都要给明确反馈
6. **先做深 1688**：在单站点体验扎实前，不急着扩平台

## 3. 明确不做或后置的方向

以下方向短期不作为主线：

- 全自动翻页 / 批量爬取
- 重型框架重构
- 一上来做 SaaS 后台与账号体系
- 多平台一把梭（淘宝 / 拼多多 / 国际站等）；通用站点适配仅作阶段 4 远期评估
- 每次抓取都自动调用大模型
- 让 AI 直接批量输出商品表，而不是修复提取规则

原因：

- 风控与维护成本高
- 偏离“人工选品助手”定位
- 会破坏当前轻量、本地、低成本的优势

## 4. 总路线图

### 阶段 1：做稳现有能力

> 状态：v1.7.0 已落地（提取稳定性、选择页改价换图、导出进度与图片尺寸、去重与文档）。

目标：先变成“能天天用、少翻车”的工具。

优先事项：

1. **页面适配稳定性**
   - 选择器多策略回退
   - 提取结果自检（0 条 / 字段大面积缺失）
   - 抓取失败时给出可理解提示
2. **选择页人工校正**
   - 同一商品常有多规格 / 多款式，默认抓到的主图和售价不一定是用户要的
   - 价格：在商品选项卡片上直接可编辑（输入框），修改后同步到导出数据
   - 图片：支持把目标图片拖入卡片缩略图区域替换；同时支持右键菜单选择“替换图片”
   - 校正结果应写回当前列表 / 收藏夹，避免导出后还是旧值
   - 可选扩展：标题等少量字段也可点选编辑，但阶段 1 先保证售价与图片
3. **导出体验**
   - 导出进度（尤其图片下载）
   - 图片尺寸可配置
   - 失败图片可重试或标记“无图”
4. **数据质量**
   - 价格 / 销量清洗
   - 按 `offerId` 去重
   - 收藏夹重复提示
5. **发布与文档**
   - 版本发布与 changelog
   - README 同步当前架构与常见问题

### 阶段 2：选品工作台

目标：从“导出工具”升级为“采购决策工具”。

高价值能力：
1. **评分与排序**
   - 价格、销量、起批等自定义权重
   - 收藏夹内状态：待看 / 候选 / 淘汰
2. **对比视图**
   - 勾选 2 到 5 个商品横向对比
3. **收藏体系增强**
   - 标签、备注
   - 本地 JSON 备份 / 恢复
4. **导出模板**
   - 采购模板 / 上架模板 / 简报模板
   - 自定义列顺序

### 阶段 3：生态扩展

目标：扩大分发与用户面，但保持产品主线不飘。

可选方向：

1. Chrome Web Store/Microsoft Edge 上架
2. 在 1688 做深后再评估多站点
3. 谨慎考虑轻量云同步（仅收藏 / 备注，不做服务端抓取）
4. AI 辅助增强（标题清洗、类目归类、卖点提炼）作为后置能力

### 阶段 4：通用站点适配（远期）

> 状态：设想 / 后置。不进入 `v1.9`、`v2.0` 主线；仅在 1688 工作台与手动 AI 修复验证充分后评估。

目标：把当前“1688 专用提取 + AI 修规则”沉淀为**用户自定义字段的通用页面适配器**，而不是重做成全网自动爬虫。

核心设想：

1. 用户打开任意列表 / 详情页
2. 声明需要的字段（如标题、价格、图片、链接、自定义列）
3. 手动触发 AI，结合字段定义分析精简 DOM 样本
4. 生成并 dry-run 校验该站点 adapter
5. 本地缓存“站点 / 页面类型 → 规则”
6. 之后在该站点继续本地提取，不再每次调模型

前置条件（不满足则不做）：

- 1688 主路径足够稳，`v1.8` AI 修规则在真实改版页可复用
- Adapter 格式、评分、缓存、回滚已 ded 成通用层
- 权限模型可控：按站点授权，而不是默认放开全网后台抓取
- 仍坚持人工触发、本地优先、失败可感知

明确边界：

- 不是“安装后自动支持全部网站”
- 不是无人值守批量爬取
- 不是把整页 DOM 每次都送给模型
- 先服务“用户正在浏览的页面”，再谈站点库分发

## 5. 版本规划

版本规划对齐第 4 节总路线图，按可独立交付的切片推进：

| 阶段 | 版本 | 主题 | 状态 |
|------|------|------|------|
| 阶段 1 | `v1.7.x` | 稳定性 / 人工校正 / 导出体验 | 已完成主体 |
| 过渡 | `v1.8` | 规则引擎 + 手动 AI 自愈 | 已完成主体 |
| 阶段 2 | `v1.9` | 收藏工作流 + 评分排序 | 已完成主体（`v1.9.1`） |
| 阶段 2 → 3 | `v2.0` | 对比 / 模板 / 工作台成型 / 商店分发 | 待开始 |

约束：

1. 每个小版本都能独立安装、日常可用
2. 2.0 前不做自动爬虫、SaaS、多平台一把梭
3. AI 只做手动触发的规则修复，不做日常自动生产线
4. 阶段 2 主线是“决策与工作流”，不为字段扩展单独开大版本；起批等字段仅在排序 / 对比真正需要时按需补

### v1.7.x（阶段 1 · 已完成）

**主题：** 稳定性、人工校正、导出体验  
**状态：** `v1.7.0` 已发布；销量识别等补丁并入 `v1.7.x`

| 能力 | 说明 | 状态 |
|------|------|------|
| 提取容错 | 多选择器 + 启发式卡片定位 | 已完成 |
| 质量自检 | 0 条 / 字段大面积缺失时提示 | 已完成 |
| 选择页改价 | 卡片上直接编辑售价并写回 | 已完成 |
| 选择页换图 | 拖入 / 右键替换，写回导出与收藏 | 已完成 |
| 导出进度 | 图片下载 / 生成表格 / 保存可感知 | 已完成 |
| 图片尺寸 | Excel 80 / 120 / 160 | 已完成 |
| 图片 URL 规范化 | 修复 `jpg.jpg_.webp` 一类 404 | 已完成 |
| 导出兼容 MV3 | data URL 优先，避免 `createObjectURL` 缺失 | 已完成 |
| `offerId` 去重 | 收藏夹新增 / 跳过提示 | 已完成 |
| 销量清洗 | 兼容 `1000+` / `1万+` / 成交文案 | 已完成（补丁） |

验收标准：

- 常见 1688 搜索结果页能稳定提取主字段
- 用户可在选择页改价、换图后再导出正确结果
- 导出失败、图片失败有明确反馈

### v1.8（过渡 · 已完成主体）

**主题：** 规则引擎化 + 手动 AI 分析模式  
**目标：** 页面改版时能自愈，而不是每次硬改选择器后发版  
**对应：** 阶段 1 收尾 → 阶段 2 之前的基础设施

| 能力 | 说明 | 优先级 |
|------|------|--------|
| Adapter Registry | 内置规则可配置、可并存、可评分择优 | P0 |
| 提取评分升级 | 用完整度 / 字段有效率在多套规则间择优 | P0 |
| 失败引导文案 | `无法正常提取信息？试试 AI 分析模式吧` | P0 |
| 手动 AI 修复 | 用户点击后，基于 2~3 个精简卡片 DOM 生成 adapter | P0 |
| dry-run 校验 | 新规则先本地试跑，优于旧规则才落盘 | P0 |
| 规则缓存 / 回滚 | AI 规则可保存；一键回退内置规则 | P0 |
| API Key 配置 | 用户自备 Key；无 Key 时不影响规则主路径 | P1 |
| 清除 AI 规则 | popup / 设置中可删除缓存 adapter | P1 |

明确不做（本版本）：

- 自动每次抓取都调模型
- 让模型直接输出整表商品 JSON 作为主路径
- 对比视图、导出模板、商店上架等阶段 2/3 能力抢跑

验收标准：

1. 内置规则失效时，用户能看到失败提示与 AI 入口
2. 手动修复成功后，同一页面可继续本地提取
3. 修复失败可回滚，不影响已有收藏与导出

### v1.9（阶段 2 前半 · 已完成主体）

**主题：** 收藏工作流 + 评分排序  
**目标：** 从“能导出”升级为“能管理候选、能按权重筛货”  
**版本：** `v1.9.1`

| 能力 | 说明 | 状态 |
|------|------|------|
| 收藏状态 | 待看 / 候选 / 淘汰 | 已完成 |
| 标签与备注 | 本地可写、可搜索 | 已完成 |
| 本地备份 / 恢复 | 收藏与备注 JSON 导入导出 | 已完成 |
| 自定义排序 / 打分 | 价格、销量权重（候选加分 / 淘汰减分） | 已完成 |
| 极简 / 专业模式 | 极简隐藏权重；专业展示完整控件；直接选取导出无加权 | 已完成（`v1.9.1`） |
| 排序所需字段按需补齐 | 起批等暂未接入；当前用价格 / 销量即可排序 | 未做（P1，按需） |
| 选择页轻编辑扩展 | 标题等少量字段可点改（可选） | 未做（P2） |

验收标准：

- 用户能按状态管理收藏，而不是全堆在一起
- 收藏夹内可按自定义权重排序
- 换机或清缓存前可备份收藏数据

### v2.0（阶段 2 后半 + 阶段 3 入口）

**主题：** 决策闭环、工作台成型、商店分发  
**目标：** 完成“浏览 → 收集 → 筛选 → 对比 → 导出”，并具备 Chrome / Edge 上架基础

| 能力 | 说明 | 优先级 |
|------|------|--------|
| 商品对比 | 勾选 2~5 个商品横向对比 | P0 |
| 导出模板 | 采购 / 上架 / 简报模板 + 列顺序 | P0 |
| 选品工作流打磨 | 默认路径更顺、空态与失败态完整 | P1 |
| 商店分发准备 | Chrome Web Store / Microsoft Edge 上架材料、权限与隐私说明 | P1 |
| 可选轻量增强 | 主线稳定后再评估：轻量云同步收藏、AI 标题清洗等 | P2 |

明确后置：

- 多平台（淘宝 / 拼多多 / 国际站）
- 服务端抓取
- 账号体系 / 重 SaaS

验收标准：

- 收藏夹内可完成对比与排序决策
- 至少两套导出模板可直接使用
- 安装 / 权限 / 隐私说明达到 Chrome 与 Edge 商店上架可读性

### v2.x+（阶段 4 · 远期，不打乱当前排期）

**主题：** 通用站点适配器  
**目标：** 用户自定义字段 + 手动 AI 识别 DOM，沉淀可复用站点规则  
**触发条件：** `v2.0` 工作台成型，且 1688 AI 修规则经验足够

建议能力切片（仅规划，不排进当前迭代）：

| 能力 | 说明 | 备注 |
|------|------|------|
| 自定义字段 schema | 用户声明字段名、类型、示例 | 替代写死 1688 字段 |
| 站点授权模型 | 仅对用户确认的域名注入 / 请求 | 避免默认全网权限 |
| 通用采样与 dry-run | 复用 v1.8 的样本 DOM / 评分 / 回滚 | 先抽象公共层 |
| 站点规则库 | `hostname + pageType → adapter` 本地缓存 | 可导出 / 导入 |
| 人工校正迁移 | 选择页改价换图等能力按字段配置复用 | 非所有站点默认开启 |
| 可选规则分享 | 后期才评估社区 / 云端规则包 | 默认仍本地 |

不在此阶段做：

- 自动发现并抓取全站
- 服务端代抓
- 把插件改名为“通用爬虫”

### 版本与阶段映射（速查）

```text
阶段 1 做稳现有能力
  └─ v1.7.x  提取稳定 / 改价换图 / 导出体验        [已完成]

过渡：页面改版自愈
  └─ v1.8    Adapter + 手动 AI 修复                 [已完成]

阶段 2 选品工作台
  ├─ v1.9    状态备注备份 + 评分排序           [已完成主体]
  └─ v2.0    对比 / 导出模板 / 工作台成型

阶段 3 生态扩展
  └─ v2.0 起按需评估：Chrome/Edge 上架、轻量同步、内容向 AI

阶段 4 通用站点适配（远期）
  └─ v2.x+  自定义字段 + 手动 AI 学 DOM + 站点规则库
```

### 发布节奏建议

| 版本 | 建议节奏 | 发布形态 |
|------|----------|----------|
| `v1.7.x` | 按需热修 | patch（销量、选择器、导出兼容） |
| `v1.8` | 1 个完整迭代 | minor：规则引擎 + AI 入口闭环 |
| `v1.9` | 1 个完整迭代 | minor：收藏工作流 + 排序打分 |
| `v2.0` | 1 个完整迭代 | major：对比 / 模板 / 分发准备 |

热修原则：

- 只修提取 / 导出 / 数据写回等阻断问题
- 不把新功能塞进 patch
- 文档与 `manifest.json` 版本号同步

## 6. AI 能力定位

### 6.1 原则

AI 是**规则修复器**，不是**日常生产线**。

正确形态：

1. 本地规则抓取（主路径）
2. 用户发现异常
3. 手动点击 AI 分析 / 修复
4. AI 基于精简 DOM 样本生成提取规则
5. 本地 dry-run 验证
6. 验证通过后缓存规则，继续本地提取

错误形态：

- 每次打开页面都自动调模型
- 每次导出都让模型直接吐商品 JSON
- 把整页超大 DOM 无差别发送给模型

### 6.2 为什么采用手动触发

手动触发更适合当前产品：

1. 用户意图明确，不误触发
2. 成本可控
3. 隐私更干净
4. 实现更简单
5. 不破坏“轻量插件”心智

推荐交互：

- 失败提示：`未能识别到商品卡片，页面结构可能已更新`
- 引导文案：`无法正常提取信息？试试 AI 分析模式吧`
- 按钮：`开始 AI 修复`
- 成功：`修复完成，已重新提取并保存规则`
- 失败：`未能自动修复，请稍后重试或反馈页面`

### 6.3 建议架构

```text
content.js
  ├─ Adapter Registry（内置规则 + AI 修复规则）
  ├─ extractByRules()
  ├─ validate(products)
  └─ sampleDOMForRepair()

background.js
  ├─ 调用 LLM（用户配置 API Key）
  ├─ 校验返回的 adapter
  └─ 写入 chrome.storage

popup / select UI
  ├─ 失败引导
  ├─ AI 修复入口
  └─ 清除 AI 规则 / 回滚内置规则
```

### 6.4 Adapter 方向

AI 输出应是可复用规则，而不是一次性商品列表。示意：

```json
{
  "version": "2026-07-11",
  "site": "1688-search",
  "card": ".search-offer-wrapper, [data-renderkey]",
  "fields": {
    "title": [".title-text", ".offer-title", "a[title]"],
    "price": [".price-item", "[class*=price]"],
    "sales": [".col-desc_after", "[class*=sale]"],
    "shop": [".desc-text", "[class*=shop]"],
    "image": ["img.main-img", "img[data-src*=alicdn]", "img[src*=alicdn]"],
    "link": ["a[href*='detail.1688.com/offer']", "a[href*='offerId=']"]
  },
  "filters": {
    "skipCard": [".cardui-adOffer", "[class*=adOffer]"]
  }
}
```

配套要求：

- 只发送精简后的 2 到 3 个样本卡片 DOM
- 修复前 dry-run
- 比旧规则更好才保存
- 保留内置规则，支持一键回滚
- 默认不强制依赖 API Key

### 6.5 AI 之前的两层兜底

在接入 LLM 前，应先具备：

1. **多规则并存**
   - 多套选择器同时尝试
   - 用结果完整度评分择优
2. **启发式提取**
   - 通过 `offer` 链接簇定位商品
   - 同卡片内找 `alicdn` 图片
   - 用文本模式识别价格 / 销量

LLM 放在这两层之后，性价比最高。

### 6.6 远期：通用站点适配

`v1.8` 已证明一条可扩展路径：

**用户需要的字段 + 精简 DOM 样本 → AI 生成 adapter → dry-run → 本地缓存**

这可以在远期推广到更多网站，但产品形态仍应是：

1. 用户指定字段
2. 用户手动触发学习 / 修复
3. 规则本地复用
4. 提取失败时再人工介入

而不是：

- 宣称支持全部网站后自动生效
- 每次浏览都调用模型
- 脱离“选品 / 列表整理”场景去做通用爬虫平台

因此：通用化是 **v1.8 架构的自然延伸**，排在 1688 做深与 `v2.0` 工作台之后，不抢当前优先级。

## 7. 优先级排序

按投入产出排序：

1. 稳定性：选择器容错、失败可感知、去重
2. 选择页人工校正：可改售价、可替换图片
3. 导出体验：进度、图片尺寸、失败重试
4. 手动 AI 修复：页面改版自愈
5. 收藏夹工作流：状态、标签、备注、备份
6. 评分 / 排序
7. 对比视图
8. 导出模板
9. Chrome / Edge 商店上架
10. AI 内容增强 / 云同步
11. **远期** 通用站点适配：自定义字段 + 手动 AI 学 DOM + 站点规则库

说明：

- 起批、发货地等采购字段不再作为独立主线；仅在排序、对比或模板真正依赖时按需补齐
- “推广到全部网站”只作为阶段 4 远期方向；在 `v1.9` / `v2.0` 完成前不启动实现

## 8. 近期执行建议

`v1.9` 主体已落地，最小推进顺序：

1. **v1.8 热修（持续）**：按真实 1688 改版页验证 AI dry-run 与规则回滚
2. **v2.0**：对比视图、导出模板
3. **v2.0**：Chrome / Edge 分发准备
4. **按需**：起批等排序字段补齐（不单独开大版本）

一句话收敛：

**先把 1688 本地选品工作台做稳做深，再用手动 AI 修复应对页面改版，而不是把项目做成自动爬虫或重 API 依赖产品。**

## 9. 当前进度

- **已完成**：阶段 1 / `v1.7.x`；过渡版 `v1.8.0`（Adapter Registry、提取评分、失败引导、手动 AI 修复 / dry-run / 缓存回滚）
- **当前版本线**：`main` @ `v1.9.1`（收藏工作流 + 极简/专业模式）
- **下一步**：`v2.0` —— 对比视图 / 导出模板 / 工作台成型 / 商店分发准备
- **更远期**：阶段 4 / `v2.x+` —— 自定义字段 + 手动 AI 学 DOM，形成通用站点规则库（不打乱当前排期）
